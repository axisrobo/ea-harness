Place the original reference architecture image here as
`reference-architecture.png` (the PlantMES plant-edge diagram with
new/existing interface markers). The image is intentionally not scrubbed and
must be treated as restricted input.

The image is used by:
- `arch-req-from-diagram` (vision extraction → partial req.yaml)
- `arch-validate` (score the reference diagram)

Do NOT commit this restricted image — see `examples/README.md` →
"Sensitive-data policy".
