Place the original reference architecture image here as
`reference-architecture.png` (the OVP ROW AWS + multi-DC hybrid diagram).

The image is used by:
- `arch-req-from-diagram` (vision extraction → partial req.yaml)
- `arch-validate` (score the reference diagram)

The reference image is intentionally not scrubbed. Treat this directory as
restricted input and do not publish it outside the authorized environment.
