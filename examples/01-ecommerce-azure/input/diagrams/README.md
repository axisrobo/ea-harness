Place the original reference architecture image here as
`reference-architecture.png` (the e-commerce Azure hub-spoke diagram). The
image is intentionally not scrubbed and must be treated as restricted input.

The image is used by:
- `arch-req-from-diagram` (vision extraction → partial req.yaml)
- `arch-validate` (score the reference diagram against azure-standard)

Do not distribute or expose this restricted image. Follow
`examples/README.md` → "Sensitive-data policy".
