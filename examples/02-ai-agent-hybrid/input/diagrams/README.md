Place the original reference architecture image here as
`reference-architecture.png` (the GDA data-agent hybrid diagram).

This is restricted input and is intentionally NOT scrubbed so the original
system names remain available for traceability.

The image is used by:
- `arch-req-from-diagram` (vision extraction → partial req.yaml)
- `arch-validate` (score the reference diagram)

Do NOT distribute or commit this restricted image — see
`examples/README.md` → "Sensitive-data policy".
