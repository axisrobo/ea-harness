"""Requirements intake: normalize, merge, and finalize partial requirements.

Canonical implementation for the read -> normalize -> merge -> validate ->
serialize pipeline that produces ``req/v1`` documents. Import from here,
not from ``tools/``.
"""
